import { RiLoader4Line } from '@remixicon/react';

const Spinner = () => (
    <div className="spinner-wrapper">
        <RiLoader4Line className="spinner" aria-label="Loading..." size={40} />
    </div>
);

export default Spinner;
